services:
  web:
    build: .
    container_name: osc-shop-web
    env_file:
      - .env
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - osc_shop_instance:/app/instance
    restart: unless-stopped

volumes:
  osc_shop_instance:
